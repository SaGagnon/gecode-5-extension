TABLE executions
exec_id uint
pb_name str
num_ex uint
ech_method uint
max_nb_nodes uint
branching_name str
#SQL date DATETIME DEFAULT (DATETIME(CURRENT_TIMESTAMP, 'LOCALTIME')),
#SQL PRIMARY KEY(exec_id)
#END WITH_ROWID

TABLE results
exec_id uint
res_id uint
var_id uint
val int
#SQL PRIMARY KEY(res_id, exec_id, var_idx)
#END WITHOUT_ROWID

#SQL create index results_idx on results (exec_id, res_id, var_idx);

TABLE nodes
exec_id uint
node_id uint
sat uint
#SQL PRIMARY KEY(exec_id, node_id)
#END WITHOUT_ROWID

#SQL create index nodes_idx on nodes (exec_id, node_id);

TABLE assigned
exec_id uint
node_id uint
var_id uint
val int
#SQL PRIMARY KEY(exec_id, node_id, var_idx)
#END WITHOUT_ROWID

TABLE densities
exec_id uint
node_id uint
var_id uint
val int
dens double
a_avg_sd double
var_dom_size uint
max_rel_sd double
max_rel_ratio double
w_sc_avg double
w_anti_sc_avg double
w_t_avg double
w_anti_t_avg double
w_d_avg double
#SQL PRIMARY KEY(exec_id, node_id, var_idx, val)
#END WITHOUT_ROWID
